server:
  port: 9999

spring:
  application:
    name: qx-gateway
  redis:
    password:
    host: 123.207.242.177
    timeout: 5000
# 配置中心
#  cloud:
#    config:
#      fail-fast: true
#      name: ${spring.application.name}
#      profile: ${spring.profiles.active}
#      discovery:
#        enabled: true
#        service-id: pig-config
#  profiles:
#    active: dev
#  main:
#    allow-bean-definition-overriding: true

# 注册中心配置
eureka:
  instance:
    instance-id: qx-user-consumer-${grpc.server.port}
    prefer-ip-address: true     #访问路径可以显示IP地址
    lease-renewal-interval-in-seconds: 3 #设置下线心跳
    lease-expiration-duration-in-seconds: 9
  client:
    service-url:
      defaultZone: http://qx:qx@10.0.75.1:7001/eureka/,http://qx:qx@10.0.75.1:7002/eureka/,http://qx:qx@10.0.75.1:7003/eureka/

