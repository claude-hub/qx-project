package ${package}.${moduleName}.provider.service.impl;

import com.baomidou.mybatisplus.extension.service.impl.ServiceImpl;
import ${package}.${moduleName}.api.entity.${className}Lang;
import ${package}.${moduleName}.provider.mapper.${className}LangMapper;
import ${package}.${moduleName}.provider.service.${className}LangService;
import org.springframework.stereotype.Service;

/**
 * @author ${author}
 * Date ${datetime}
 */
@Service
public class ${className}LangServiceImpl extends ServiceImpl<${className}LangMapper, ${className}Lang> implements ${className}LangService {

}
